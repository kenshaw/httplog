name: Test
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
    - name: Install Go
      uses: actions/setup-go@v3
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Test
      run: go test -v ./...
